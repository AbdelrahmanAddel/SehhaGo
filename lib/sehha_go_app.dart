import 'package:flutter/material.dart';

class SehhaGo extends StatelessWidget {
  const SehhaGo({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: const Placeholder());
  }
}
